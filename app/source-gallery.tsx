export default function SourceGallery({base,items}:{base:string;items:{file:string;caption:string}[]}){return <div className="field-gallery source-preserve">{items.map(item=><figure className="field-photo" key={item.file}><a href={`${base}/${item.file}`} target="_blank" rel="noopener noreferrer"><img src={`${base}/${item.file}`} alt={item.caption} loading="lazy" /></a><figcaption>{item.caption}</figcaption></figure>)}</div>}
